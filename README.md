# HTTP Requests Demo

A project demonstrating how to interact with external APIs in React.

## Features
- Data fetching from a remote backend.
- Handling loading and error states.
- Sending POST requests (e.g., for user selections).
- Optimized data fetching patterns.

## Getting Started
1. Clone the repository.
2. Run `npm install`.
3. Run `npm run dev`.
